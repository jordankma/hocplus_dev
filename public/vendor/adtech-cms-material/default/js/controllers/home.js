'use strict';

/**
 * @ngdoc function
 * @name demoApp.controller:IndexCtrl
 * @description
 * # IndexCtrl
 * Controller of the demoApp
 */
angular.module('AdtechApp')

    .controller('HomeCtrl', [
        '$scope',
        '$timeout',
        '$state',
        'ssSideNav',
        function (
            $scope,
            $timeout,
            $state,
            ssSideNav) {


        }
    ]);